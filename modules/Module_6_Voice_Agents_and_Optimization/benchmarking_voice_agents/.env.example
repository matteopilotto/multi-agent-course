# Copy to `.env` (same folder) and fill in your key. Used by the LLM-as-judge.
#   cp .env.example .env

# OpenAI key for grading the text responses (LLM-as-judge).
OPENAI_API_KEY=sk-...

# Judge model — a recent OpenAI model. Change if you use a different one.
OPENAI_JUDGE_MODEL=gpt-5
