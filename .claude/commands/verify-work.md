Run the `verify-work` skill.

End-of-session code review — checks for unused imports, console.logs, missing error handling, hardcoded values, and edge cases before committing. Use when the user is done coding and wants a final pass before committing or shipping.

$ARGUMENTS
