Run the `changelog-writer` skill.

Read the git log and write a user-facing changelog entry in Keep a Changelog format. Use when the user wants to document what changed in a release, write release notes, or update their CHANGELOG.md.

$ARGUMENTS
