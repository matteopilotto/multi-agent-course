Run the `feature-prioritizer` skill.

Score a feature list using RICE (Reach, Impact, Confidence, Effort) and return a ranked table with recommendation. Use when the user wants to prioritize features, decide what to build next, or cut scope.

$ARGUMENTS
