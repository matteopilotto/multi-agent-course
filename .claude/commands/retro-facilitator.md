Run the `retro-facilitator` skill.

Generate a sprint retrospective document from raw notes or observations. Returns what worked, what didn't, and 3 concrete action items with owners. Use when the user wants to run or document a sprint retro.

$ARGUMENTS
