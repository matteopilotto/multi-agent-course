Run the `frontend-slides` skill.

Create animation-rich, single-file HTML presentations from scratch or by converting a PowerPoint (.pptx) file. Shows 3 visual style previews before building the full deck — no design experience needed.

$ARGUMENTS
