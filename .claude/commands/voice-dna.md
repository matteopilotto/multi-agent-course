Run the `voice-dna` skill.

Analyze writing samples and extract the patterns that define the user's voice. Use when the user wants to capture their writing style, train Claude on how they write, or make future writing sound like them.

$ARGUMENTS
